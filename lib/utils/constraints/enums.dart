

  /// List of Enums
  /// Cannot be created inside a class
library;


  enum CarType { single, variables }

  enum GuideType { single, group }

  enum TextSizes { small, medium, large }

  enum BookingType { carOnly, guideOnly, carAndGuide }

  enum BookingStatus { pending, confirmed, inProgress, completed, cancelled }

  enum PaymentMethods { stripe, paypal, googlePay, applePay, visa, masterCard, creditCard, paystack, razorPay, paytm }
